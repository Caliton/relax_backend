import { Body, Controller } from '@nestjs/common';

@Controller('requeststatus')
export class RequestStatusController {
  // @Get()
  // async index(): Promise<RequestStatus[]> {
  //   return this.requeststatusService.findAll();
  // }
  // @Post()
  // async create(@Body() body: RequestStatus) {
  //   return await this.requeststatusService.create(body);
  // }
  // @Put(':id')
  // async update(@Param('id') id: string, @Body() body: RequestStatus) {
  //   return await this.requeststatusService.update(id, body);
  // }
  // @Delete(':id')
  // async destroy(@Param('id') id: string) {
  //   await this.requeststatusService.deleteById(id);
  // }
}
